#Libraries
library(parallel)
library(data.table)
library(reshape2)
library(purrr)

#functions
p = function(...){paste(...,sep="")}
s = function(...){source(...)}
d = function(...){dim(...)}
dn = function(...){dimnames(...)}
